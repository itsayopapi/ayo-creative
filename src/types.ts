export type IndustryPreview = {
  id: string;
  name: string;
  category: string;
  region: string;
  tagline: string;
  accentColor: string;
  bgColor: string;
  textColor: string;
  img: string;
  navItems: string[];
  heroHeadline: string;
  heroCopy: string;
  ctaLabel: string;
  pill: string;
};